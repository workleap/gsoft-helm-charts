# Migration Guide

This chart has moved.

`aspnetcore` 5.1.0 is the final published release here. Subsequent versions ship as **`leap-chart`** in [`workleap/wl-helm-charts`](https://github.com/workleap/wl-helm-charts), distributed via OCI at `oci://ghcr.io/workleap/charts/leap-chart`.

The canonical migration guide — including the `aspnetcore` 5.1 → `leap-chart` 6.0 step-by-step and the historical 4→5 / 3→4 sections — lives at:

**[`wl-helm-charts/charts/leap-chart/MIGRATION.md`](https://github.com/workleap/wl-helm-charts/blob/main/charts/leap-chart/MIGRATION.md)**
